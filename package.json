{
  "name": "awesomeedwin",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler pages deploy --branch main --commit-dirty=true",
    "deploy:preview": "wrangler pages deploy --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
